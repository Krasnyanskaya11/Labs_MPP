<p style="color: green"><%= notice %></p>

<%= render @clinic %>


<div>
  <p class="notice"><%= notice %></p>
  <div class="show-info">
  <h1><%= @clinic.name %></h1>
  <p><strong>ID:</strong> <%= @clinic.id %></p>
  <p><strong>Address:</strong> <%= @clinic.address %></p>
  <p><strong>Phone:</strong> <%= @clinic.phone %></p>
    <p><strong>Year:</strong> <%= @clinic.year_founded%></p>
  </div>


  <p><%= link_to "Edit this clinic", edit_clinic_path(@clinic) %> |
  <%= link_to "Back to clinics", clinics_path %>
  </p>
  <%= button_to "Destroy this clinic",@clinic, method: :delete ,class: "button",turbo_confirm: "Are you sure?"%>

</div>

<%= link_to 'До пацієнтів клініки', patient_clinics_path %>