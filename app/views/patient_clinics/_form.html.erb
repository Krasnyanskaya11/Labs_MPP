<%= form_with(model: patient_clinic) do |form| %>
  <% if patient_clinic.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(patient_clinic.errors.count, "error") %> prohibited this patient_clinic from being saved:</h2>

      <ul>
        <% patient_clinic.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
